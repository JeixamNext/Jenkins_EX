<!DOCTYPE html>
<html>
<head>
  <title> tabla de base de datos </title>
  <style>
   table {
    border-collapser:collapse;
    width: 100%;
    color: #588c7e;
    font-family:monospace;
    font-size:25px;
    text-align: left;
      }
   th { 
    background-color: #588c7e;
    color: white;
     }
   tr:nth-child(even) {background-color: #f2f2f2}
  </style>
</head>
<body>
  <table>
  <tr>
   <th>id</th>
   <th>name</th>
   <th>age</th>
  </tr>
  <?php
   $conn=mysql_connect("db","root","123","people");
   //check connection
   if($conn->connect_error)
   {
     die("connection faild: ". $conn->connect_error);
   }
  $sql="SELECT id,name,lastname,age FROM registrer where age=20";
  $result=$conn->query($sql);
  if($result->num_rows>0)
  {
    //output data
    while ($row=$result->fetch_assoc()){
     echo "<tr><td>" . $row["id"]. "</tr><td>" . $row["name"]. "</tr><td>"
     . $row["lastname"]. "</tr><td>"  . $row["age"]. "</tr><td>";
    }
    echo "</table>";
  } else {echo "no hay resultados"}
  $conn->close();
?>
</table>
</body>
</html>